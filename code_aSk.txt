# pyright: ignore

select(Note).filter(Note.id == note_id)
result.scalars().one()
https://github.com/fastapi-users/fastapi-users/blob/master/fastapi_users/schemas.py



# Beanie

Defining a document
The Document class in Beanie is responsible for mapping and handling the data from the collection.
It is inherited from the BaseModel Pydantic class, so it follows the same data typing and parsing behavior.
