apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tech-stack.fullname" . }}-test-connection"
  labels:
    {{- include "tech-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tech-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
