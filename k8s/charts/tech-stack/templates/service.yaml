apiVersion: v1
kind: Service
metadata:
  name: {{ include "tech-stack.fullname" . }}
  labels:
    {{- include "tech-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tech-stack.selectorLabels" . | nindent 4 }}
